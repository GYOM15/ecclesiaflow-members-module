<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Silence logging aspects during tests -->
    <logger name="com.ecclesiaflow.application.logging.aspect" level="OFF"/>
    
    <!-- Silence gRPC client logging during tests -->
    <logger name="com.ecclesiaflow.io.grpc" level="WARN"/>
    
    <!-- Keep Spring and Hibernate quiet -->
    <logger name="org.springframework" level="WARN"/>
    <logger name="org.hibernate" level="WARN"/>
    <logger name="io.grpc" level="WARN"/>
    
    <!-- Application logs at INFO level -->
    <logger name="com.ecclesiaflow" level="INFO"/>

    <root level="WARN">
        <appender-ref ref="CONSOLE"/>
    </root>
</configuration>
